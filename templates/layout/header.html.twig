

<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home') }}">
       <img src="{{ asset('img/zack.png') }}" alt="" width="30" height="24" class="d-inline-block align-text-top">MyProject</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav me-auto">
            <li class="nav-item active">
            <a class="nav-link" href="{{ path('home') }}">Startseite
                <span class="visually-hidden">(current)</span>
            </a>
            </li>
            <li class="nav-item">
            <a class="nav-link" href="{{ path('blogs_index') }}">Blogs</a>
            </li>
            <li class="nav-item">
            <a class="nav-link" href="{{ path('contact') }}">Kontakt</a>
            </li>
            <li class="nav-item">
            <a class="nav-link" href="{{ path('about') }}">Über uns</a>
            </li>
        </ul>
      
        {# <div class='col-3'>
            <form class="d-flex float-start">
                <input class="form-control me-sm-2" type="search" placeholder="Suchen" aria-label="Search">
                <button class="btn btn-secondary my-2 my-sm-0" type="submit">Suchen</button>
            </form>
        </div> #}


   
        <div class='col-2 '>
            <div class="float-end ml-3">
            {% if  app.user %}
            <a tabindex="0" class="btn btn-lg btn-light" role="button" data-bs-toggle="popover" data-bs-trigger="focus"  data-bs-html="true"
            title=" Hallo {{app.user.lastname}}" 
            data-bs-content="<div>E-Mail: {{app.user.username}} </div><br><a class='btn btn-danger' href='{{ path('app_logout') }}'> Abmelden</a>"> <i class="fas fa-user"></i> {{app.user.lastname}}</a>
                {# </div> #}
            {% else %}
                {# <div class="float-end ml-3"> #}
                    <a class="btn btn-danger" href="{{ path('app_login') }}"> Anmelden</a>
                    {# <a class="btn btn-danger" href="{{ path('app_register') }}"> Registrieren</a> #}
                   
            {% endif %}
             </div>
        </div>
    </div>
  </div>
</nav>
  
